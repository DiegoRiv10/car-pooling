# Entrega Sesion 4 - Escaneo de Figma y Modelado JSON

## Objetivo de la sesion

Escanear dos pantallas clave del MVP, identificar los campos de informacion que requiere cada una y modelar los datos en archivos `.json` dentro del repositorio.

## Pantallas trabajadas

| Pantalla | Archivo |
|----------|---------|
| Registro y verificacion de usuario | `architecture/registro_usuario.json` |
| Busqueda de viajes disponibles | `architecture/busqueda_viajes.json` |

## Checklist de entrega

- [x] Elegir dos pantallas clave del MVP.
- [x] Listar los campos requeridos por cada pantalla.
- [x] Crear un archivo `.json` por pantalla.
- [x] Mapear la estructura de datos que viaja de interfaz a servidor.
- [x] Documentar en `architecture/README.md` el metodo HTTP correspondiente.
- [ ] Mover tareas pasadas de diseno a `Done` en Jira.
- [ ] Crear o iniciar el nuevo paquete de tareas tecnicas en `In Progress`.
- [ ] Integrar cambios a `main` mediante Pull Request.

## Notas para Jira

Mover a `Done` las tareas relacionadas con diseno y prototipo de sesiones anteriores, por ejemplo:

- Crear prototipo de pantallas principales.
- Disenar interfaz principal del dashboard.
- Disenar prototipo UI/UX en Figma.

Crear o mover a `In Progress` las tareas tecnicas de esta sesion:

- Escanear pantalla de registro.
- Crear `registro_usuario.json`.
- Escanear pantalla de busqueda.
- Crear `busqueda_viajes.json`.
- Documentar metodos HTTP en `architecture/README.md`.
